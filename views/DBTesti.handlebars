<h1>{{title}}</h1>

{{#if movies.length}}
  <ul>
    {{#each movies}}
      <li>
        <strong>{{Name}}</strong> ({{Year}}) — {{category}}
        <p>{{Description}}</p>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No movies found.</p>
{{/if}}
